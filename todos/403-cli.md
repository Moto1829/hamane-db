# 403: hamane-cli

- Status: DONE (2026-07-12)
- Milestone: M4
- Depends: 305
- Design: DESIGN.md §7

## ゴール

動作確認・デバッグ・デモに使える最小限の CLI を提供する。

## やること

- [ ] `crates/hamane-cli` (bin name: `hamane`)。deps: clap, serde_json
- [ ] サブコマンド:
  - `hamane create <db> <collection> --dim N --metric l2|cosine|dot`
  - `hamane insert <db> <collection>` — JSONL を stdin から
    (`{"id": 1, "vector": [...], "meta": {...}}`)
  - `hamane search <db> <collection> --k 10 --ef 64 [--filter '<json>'] --vector '[...]'`
  - `hamane info <db>` — collection 一覧・件数・セグメント構成
  - `hamane flush / compact <db> <collection>`
- [ ] フィルタの JSON 表現 (`{"eq": ["lang", "ja"]}` 等) → Filter 変換
- [ ] 出力は JSON (機械可読) + `--pretty`

## 完了条件

- create → insert (1 万件) → search → info の一連が手元で動く
- README.md に使用例を記載
