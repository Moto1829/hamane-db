# 000: CI (GitHub Actions)

- Status: DONE (2026-07-12)
- Milestone: 横断
- Depends: なし
- Design: —

## ゴール

push / PR ごとにテスト・lint が自動で走り、main を常に green に保つ。

## やること

- [ ] `.github/workflows/ci.yml` を作成
  - `cargo test --workspace`
  - `cargo clippy --workspace --all-targets -- -D warnings`
  - `cargo fmt --check`
- [ ] stable ツールチェーン固定 (`dtolnay/rust-toolchain@stable`) + `Swatinem/rust-cache`
- [ ] macOS / Linux の 2 OS で実行 (mmap・rename の挙動差を拾うため)

## 完了条件

- main への push で CI が走り green になる
